version: "3.9"

services:
  # -------------------------------------------------------
  # PostgreSQL Database
  # -------------------------------------------------------
  db:
    image: postgres:17-alpine
    container_name: flask-postgres-dev
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Soumya1997
      POSTGRES_DB: Flask_App2
    ports:
      - "5433:5432"
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ism-network

  # -------------------------------------------------------
  # Redis Cache
  # -------------------------------------------------------
  redis:
    image: redis:7-alpine
    container_name: flask-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis_data_dev:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ism-network

  # -------------------------------------------------------
  # Flask Backend (Gunicorn)
  # -------------------------------------------------------
  backend:
    image: snayak97/ism_project_backend_dev:${VERSION}
    container_name: flask-backend-dev
    expose:
      - "5000"   
    env_file:
      - ./Backend/.env
    environment:
      DATABASE_URL: postgresql://postgres:Soumya1997@db:5432/Flask_App2
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - ism-network

  # -------------------------------------------------------
  # Backend NGINX (Reverse Proxy for Flask)
  # -------------------------------------------------------
  backend_nginx:
    image: nginx:latest
    container_name: flask-backend-nginx-dev
    ports:
      - "5000:80"   
    volumes:
      - ./Backend/nginx/nginx.conf:/etc/nginx/nginx.conf:ro

    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - ism-network

  # -------------------------------------------------------
  # Vite Frontend
  # -------------------------------------------------------
  frontend:
    image: snayak97/ism_project_frontend_dev:${VERSION}
    environment:
      MODE: ${MODE:-local}   
    container_name: ismsoft-frontend-dev
    expose:
      - "5173"
    env_file:
      - ./fontend/.env
    depends_on:
      - backend_nginx
    restart: unless-stopped
    networks:
      - ism-network

  # -------------------------------------------------------
  # Frontend NGINX (Serves React build)
  # -------------------------------------------------------
  frontend_nginx:
    image: nginx:latest
    container_name: frontend-nginx-dev
    ports:
      - "5173:80"   # public website URL
    volumes:
      - ./fontend/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./fontend/dist:/usr/share/nginx/html

    depends_on:
      - frontend
    restart: unless-stopped
    networks:
      - ism-network


volumes:
  postgres_data_dev:
  redis_data_dev:

networks:
  ism-network:
    driver: bridge

