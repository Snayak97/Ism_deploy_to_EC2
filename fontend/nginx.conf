server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # API goes to backend
    location /api/ {
        proxy_pass http://backend:5000/;
    }

    # React SPA routing
    location / {
        try_files $uri /index.html;
    }
}
